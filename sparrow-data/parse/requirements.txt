rich
# git+https://github.com/huggingface/transformers.git
transformers==4.57.3
torchvision==0.24.1
torch==2.9.1
sentence-transformers==5.2.0
numpy==2.2.6
pypdf==6.4.2
gradio_client==2.0.1
pdf2image==1.17.0
# mlx==0.30.0; sys_platform == 'darwin' and platform_machine == 'arm64'
# mlx-vlm==0.3.9; sys_platform == 'darwin' and platform_machine == 'arm64'
ollama==0.6.1

# MLX dependencies moved to setup.py extras_require
# Install with: pip install sparrow-parse[mlx] (Apple Silicon only)

# Force reinstall:
# pip install --force-reinstall -r requirements.txt

# For pdf2image, additional step is required:
# brew install poppler